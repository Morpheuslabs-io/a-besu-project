{
  "name": "deployment",
  "version": "1.0.0",
  "description": "",
  "main": "deployContracts.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf ./build",
    "build": "npm run clean && npx truffle compile --config ./truffle-config.js",
    "deploy": "npx truffle migrate --reset --config truffle-config.js --network besu",
    "deploy-ganache": "npx truffle migrate --reset --config truffle-config.js",
    "ganache": "npx ganache-cli -l 10000000 -d 'torch almost feed brother seat earth scorpion panther nuclear interest toy guide'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@truffle/hdwallet-provider": "^1.2.5",
    "dotenv": "^8.2.0",
    "fs": "0.0.1-security",
    "keythereum": "^1.2.0",
    "solc": "0.8.2",
    "truffle": "5.2.1",
    "web3": "^1.3.4"
  },
  "devDependencies": {
    "ganache-cli": "^6.12.2"
  }
}
